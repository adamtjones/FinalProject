(function() {
    'use strict';
    angular
        .module('flowers')
        .factory('back', function($http, Backand) {

            /*var postInfo = function(data) {
                return $http({
                    method: 'POST',
                    data: data,
                    url: Backand.getApiUrl() + '/1/objects/posts',
                });

                return gettingData;
            }
            return {

                postInfo,
            }*/
        });
})();

